# Julian Bjørgen React app

See the [project README](../README.md) for local setup, Firebase configuration,
security rules and deployment.

Common commands:

```bash
npm start
npm test -- --watchAll=false
npm run build
```
